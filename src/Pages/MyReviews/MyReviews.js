import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReviews = () => {
    useTitle('My Reviews')
    return (
        <div>
            reviews
        </div>
    );
};

export default MyReviews;