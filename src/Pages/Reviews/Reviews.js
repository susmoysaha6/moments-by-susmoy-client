import React from 'react';
import useTitle from '../../hooks/useTitle';

const Reviews = () => {
    useTitle('My Reviews')
    return (
        <div>
            reviews
        </div>
    );
};

export default Reviews;