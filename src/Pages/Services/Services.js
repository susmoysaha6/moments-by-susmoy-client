import React from 'react';
import useTitle from '../../hooks/useTitle';

const Services = () => {
    useTitle('Services')
    return (
        <div>
            service
        </div>
    );
};

export default Services;